<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Démo Ansible + GitHub Pages</title>
  <style>
    body { font-family: sans-serif; margin: 2em; text-align: center; }
    img { max-width: 80%; height: auto; margin-top: 2em; }
    a.button { display: inline-block; margin: 1em; padding: 0.5em 1em; background: #007bff; color: white; text-decoration: none; border-radius: 4px; }
    a.button:hover { background: #0056b3; }
  </style>
</head>
<body>
  <h1>Démo Ansible + GitHub Pages</h1>
  <p>Date de déploiement : {{ deploy_date }}</p>
  <p>Commit : {{ commit_hash }}</p>
  <img src="{{ demo_image }}" alt="Image de démo">
  <br>
  <a href="" onclick="location.reload();" class="button">🔄 Rafraîchir la page</a>
  <a href="https://github.com/{{ github_repo }}" class="button" target="_blank">Voir le dépôt GitHub</a>
</body>
</html>
